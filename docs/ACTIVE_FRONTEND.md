# Active 21 Hold'em frontend

**18 September 2026 update:** The user approved 21UCD as the main public site and explicitly authorised replacing WEBDEV with its complete snapshot. WEBDEV now serves as the production promotion branch for `https://21-holdem.com`; `main` remains unchanged. See [WEBDEV-PROMOTION.md](WEBDEV-PROMOTION.md). The desktop-only and mobile-only restrictions in the historical notes below are superseded by this approval.

This is the `21UCD` (User Centered Design) branch at `D:/BIG-SLICK-GAMES/GAMES/21holdem-21UCD`. It snapshots the current working source for isolated mobile accessibility work. The desktop and deployment history below describes the preserved `21WEBDEVNEXT` baseline, not a UCD deployment. See `docs/21UCD.md` before changing layouts or preview services.

The user confirmed the black-and-gold site with the orange-jacket host, top-bar login, tutorial and animated daily reward chest is the intended frontend.

- Repository: `BIG-SLICK-GAMES/21holdem`
- Branch: `21WEBDEVNEXT`
- Local checkout: `D:\BIG-SLICK-GAMES\GAMES\21holdem`
- Application source: `frontend/`
- Local source preview: `http://192.168.0.109:3100/lobby`, served by Docker container `holdem-game-frontend`.
- Confirmed source commit: `8f771bba8bc5b040bc9a819409e96e635da56737`.

`21WEBDEV` and `WEBDEV` are different branches. The `21holdem-WEBDEV/website` Next application and the untracked `website/` directory in this checkout are not the selected site. Do not use them as the latest frontend based on their folder names or file modification times.

Production address: `https://21-holdem.com`. Deploy the confirmed branch's `frontend` Docker production build and retain the existing API and Socket.IO proxies.

Updated on 13 September 2026 (Brisbane) to `/var/www/html/releases/21webdevnext-single-room-20260913/build` on `52.65.47.126`. Apache serves it through the `/var/www/html/game_build/build` symlink. The live entry bundle is `main.b423d9a3.js`. This includes local session-recovery, mobile-header, cosmetic-shop and game-theme changes on top of `8f771bb`; those edits are not yet committed. Previous release: `/var/www/html/releases/21webdevnext-room-scale-20260912/build`.

The theme fix forwards the equipped theme through Boot into Preload, scopes the game profile cache to the signed-in token, and prevents the preload timeout from starting the game before the required table images arrive. A newly generated emerald felt/black leather/gold default table has a matching full-screen room. The room is rendered once behind the entire viewport, including desktop side areas. All five shop tables have separate transparent sprites; their original combined images remain the shop previews. Runtime artwork uses WebP with alpha preserved (12 assets: 27.6 MB PNG -> 3.8 MB WebP); PNG originals are retained in the project. Except for Riverboat, theme backgrounds still use the upper room area of the shop images to avoid a second oversized table behind the playfield.

Broken-table investigation on 12 September: the previous `static/media/portrate_table.c70300e3eb0c9932b3af.png` URL returned the SPA HTML because the release switch removed old hashed assets. Restored 14 missing static assets from the session/cosmetics releases as hardlinks. Future deployments must preserve previous `static/` files alongside the new build so open tabs can finish loading their original bundle and image URLs. The public `.htaccess` declares `image/webp` and makes HTML revalidate; this was applied to the live release and verified. Do not remove these compatibility assets on the next deployment.

Single-room release (13 September): gameplay no longer mounts the shared `.main-layout-background` container. MainLayout mounts a dedicated `.game-room-viewport` containing one `.game-room-art` image layer, with all existing BG Adjuster values applied there. The obsolete gameplay background rule and image pseudo-element were removed. Docker build and packaged-browser tests passed: exactly one room image with 50% scale and x/y movement, persisted on reload, and restored by Reset. Test uses mocked account data with sockets disconnected. Artifact `.codex-runtime/holdem-single-room.tgz` is a JS/CSS/HTML/manifest delta against room-scale; all artwork and historical asset hashes are retained. No new artwork or account changes were made.

Riverboat background scale correction: `riverboat-room-v2.webp` is a landscape room-only edit of the existing shop scene. The game uses centered `cover` sizing instead of the previous forced `300vh` crop. The fixed room is painted once on the dedicated `.game-room-art` element; `.game-room-viewport` provides a solid backing. The separate equipped table sprite is unchanged. See `docs/riverboat-room-v2.md` for asset paths and the edit prompt. Docker production build and local browser checks passed at 1440×900, 1625×1536 and 390×844. Deployment artifact `.codex-runtime/holdem-room-scale.tgz` is a delta against the table-recovery release: new compiled JS/CSS, HTML/manifest and room PNG/WebP. Deployment clones the previous release using hardlinks and replaces changed files safely, preserving all existing asset hashes. Full current build also exists in `holdem-theme-rebuild-final:/app/build`.

The table-recovery release also guards loader completion against missing table textures, retries the selected artwork as PNG, and offers an in-game retry when both formats fail. It never starts Level with a missing table or silently discards the equipped choice after an image error. `frontend/scripts/test-table-loading.cjs` checks failed downloads, retries, slow loads and classic startup; it passed in Docker. The Docker production build passed. Browser checks with deliberately blocked table WebP requests passed for Riverboat and classic on both Docker preview and the live site. These rendering checks use mocked account data and disconnected game sockets. Artifact: `.codex-runtime/holdem-table-recovery.tgz`; build output: `.codex-runtime/table-recovery-build`; deployment script preserves old static hashes via hardlinks. Source changes remain uncommitted.

The final production package was compiled in Docker using the cached builder and updated frontend source/assets. Artifact: `.codex-runtime/holdem-theme-fix-20260912.tgz` in the workspace root; runtime image: `holdem-theme-fix-20260912-final`. Startup and delayed-artwork regression checks passed; browser rendering checks passed locally and on the live site for a purchased theme and the default at desktop and phone sizes. These use isolated mocked profile data and Phaser's Canvas fallback for headless verification, without charging or altering an account. This release only updates the frontend.

The shop sells five combined table-and-room themes for 500 chips each: Timber Saloon, Riverboat Lounge, Art Deco Club, Neon Skyline, and Grand Casino. Images were copied from the older Next prototype into `frontend/public/images/shop`. The canonical purchasable catalogue is `backend/app/routers/game/shop/lib/cosmetics.json`; chip packages and the daily reward pool remain separate. Purchases atomically debit chips and save ownership on the user document, with repairable transaction receipts. Equipped themes persist in the account and load at the next game table, falling back to the classic texture if an image fails.

Only the cosmetic shop routes/service/catalogue, user schema and authentication projection were deployed to the live backend; unrelated local poker and bot edits were excluded. Original backend files are archived in the release's `backend-before.tgz`. Backend Mongo concurrency/ownership/ledger tests passed, as did local and live shop browser checks (real catalogue/images, simulated purchase/equip/reset flows without charging a real account), mobile layout checks, production build and four session regression tests.

The session fix clears a rejected current token even on the public lobby and updates the header, lobby and daily chest immediately. Late responses from older sessions cannot clear a newer login. Expired-session recovery and chest preview were checked locally and live; four interceptor regression tests passed. Preview testing did not claim rewards or credit chips.

Validation: clean-release safety and syntax checks, production build, five tutorial tests, browser checks of the black-and-gold lobby/top login/registration, matching live bundle, rewards preview API and Socket.IO handshake.
